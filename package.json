{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "build": "pulp build --include test --main Example --to phantom-script.js && rimraf docs && pulp docs"
  },
  "devDependencies": {
    "phantomjs-prebuilt": "^2.1.14",
    "pulp": "^11.0.0",
    "rimraf": "^2.4.1"
  }
}
