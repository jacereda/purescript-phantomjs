{
  "private": true,
  "scripts": {
    "postinstall": "bower update",
    "build": "pulp build --include test --main Example --to phantom-script.js"
  },
  "devDependencies": {
    "phantomjs-prebuilt": "^2.1.14",
    "pulp": "^11.0.0",
    "rimraf": "^2.4.1"
  }
}
